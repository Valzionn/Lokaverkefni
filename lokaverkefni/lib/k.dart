import 'package:flutter/material.dart';

class GarfieldApp extends StatelessWidget {
  const GarfieldApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
